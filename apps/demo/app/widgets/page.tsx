import { Catalog } from '@/components/catalog';

export const metadata = {
    title: 'Widgets · PMXT',
    description:
        'Production-grade React widgets for prediction markets. Search, charts, order books, and a full non-custodial trading flow — copy, customize, paste.',
};

export default function WidgetsPage() {
    return <Catalog />;
}
